export function createPoll({ fields }: { fields: any }) {
  // const path = "/api/papp/users/my_profile";
  // return patchWithMultipart(path, fields);

  return Promise.resolve({ success: true });
}
